# frozen_string_literal: true

RSpec.describe NatureRemoE do
  it "has a version number" do
    expect(NatureRemoE::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
